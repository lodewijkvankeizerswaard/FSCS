FACT Vragen:

# Data
Data Pre-processing compleet onduidelijk
Grote of kleine dataset? (klein)
CheXpert has multiple diseases one hot encoded. Should we merge them to make the classifier binary?
    - They use Pleural Effusion

# Training:
Training split tussen:
 - group specific models
 - feature extractor + joint classifier
    - They alternate training between group specific and general. We should do the same (or contribute doing it at the same time)
De auteurs gebruiken de grote CheXpert dataset. Help 
    - Niet waar

# Model:
BERT versie voor Civil Comments?
Joint Classifier hoeveel lagen? parallel of sequentieel? Wat betekent “curly(D) + 1” ?
    - Dit vond Dimitiris ook vaag. Hij zei, probeer gewoon een paar dingen (waarschijnlijk niet parallel), en als je er echt niet uitkomt mail de authors.
Sigmoid gebruiken?
    - Ja
Altijd SELU gebruiken na featurerizen? Ook voor pre-defined models
    - Je moet altijd iets hebben om Phi te representeren. Dit betekend niet dat dat altijd FC + SELU is, en je moet dus per dataset/task lezen wat ze hebben gedaan.
    
    
Sessie 2:

Hoe zou je het model kunnen ontwerpen om met continuous protected variables om te gaan?
